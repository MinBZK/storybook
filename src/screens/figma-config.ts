/**
 * Figma configuration for Screen Design compositions.
 *
 * Points to the "RR Library design" Figma file, which contains
 * full screen compositions showing how components are composed
 * into complete pages. This is separate from the component library
 * file used in component-level FigmaComparison stories.
 */
export const FIGMA_TOKEN = import.meta.env.STORYBOOK_FIGMA_TOKEN || '';
export const DESIGN_FILE_ID = 'yfECvY94Ky20Q7tjaaHAVA';
